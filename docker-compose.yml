version: '3.9'

services:
  nginx:
    image: nginx:latest
    network_mode: host
    restart: always
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./cert:/root/cert
      - ./nginx/logs:/var/log/nginx

  
  # web:
  #   image: python 
  #   network_mode: host
  #   restart: always
  #   volumes:
  #     - ./getip.py:/getip.py:ro
  #     - ./start.sh:/start.sh:ro
  #   command: bash /start.sh
  #   env_file:
  #     - ./.env

  web:
    image: war7ng/go-web:latest
    network_mode: host
    restart: always
    env_file:
      - ./.env
 


  xui:
    image: enwaiax/x-ui
    container_name: x-ui
    volumes:
      - ./db/:/etc/x-ui/
      - ./cert/:/root/cert/
    restart: always
    network_mode: host
